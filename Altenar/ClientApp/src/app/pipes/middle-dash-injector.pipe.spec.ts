import { MiddleDashInjectorPipe } from './middle-dash-injector.pipe';

describe('MiddleDashInjectorPipe', () => {
  it('create an instance', () => {
    const pipe = new MiddleDashInjectorPipe();
    expect(pipe).toBeTruthy();
  });
});
